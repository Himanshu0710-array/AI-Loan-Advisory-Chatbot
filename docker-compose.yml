version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./documents:/app/documents
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
